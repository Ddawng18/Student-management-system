# Phase 2 - Commands and workflow prep

This phase is design only. No runtime commands are required yet.

Preparation commands for next phase:

```bash
# From repository root
mkdir -p src
mkdir -p web-client
```

```bash
# Optional: verify .NET SDK and Node.js
 dotnet --version
 node --version
 npm --version
```

Phase 3 will execute actual scaffold commands for:

- ASP.NET Core Web API projects and solution structure
- EF Core package installation
- Node.js frontend static server setup
